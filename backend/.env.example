DATABASE_URL=mysql+pymysql://root:sumit13@localhost:3306/uni_photos
SECRET_KEY=change-this-secret-key-in-production
# Set DEBUG=true to see detailed error messages (e.g. for login issues)
# DEBUG=false
FACE_MATCH_TOLERANCE=0.45
# all_models = RetinaFace + CNN + HOG + OpenCV + low-light & sharpen (best for groups, low light, blur)
# dlib_best = CNN+HOG+OpenCV (no RetinaFace)
# retinaface_dlib = RetinaFace + dlib
FACE_DETECTION_MODEL=all_models
MAX_FACES_PER_IMAGE=25
# 1=fast, 2-3=find more small/distant faces in groups (slower)
FACE_UPSAMPLE=2
MAX_UPLOAD_SIZE_MB=10
# Total storage limit in GB (all events combined)
TOTAL_STORAGE_LIMIT_GB=4

# SMS OTP via Twilio Verify (optional; dev mode logs OTP if not set)
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_VERIFY_SERVICE_SID=VA...   # From Twilio Verify Console

